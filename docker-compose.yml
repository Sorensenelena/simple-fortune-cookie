services:
  backend:
    build: ./backend
    ports:
      - "9000:9000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    restart: unless-stopped
